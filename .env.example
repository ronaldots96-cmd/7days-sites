# Current public EasyPanel URL without a trailing slash.
SITE_URL=https://your-temporary-url.example

# Keep false while validating on the temporary URL. Change to true on the final domain.
SITE_INDEXABLE=false

# Increase this value whenever a long-cached static asset is replaced.
ASSET_VERSION=20260826
